apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 3000       # port inside the cluster
      targetPort: 3000 # port your container listens on
      nodePort: 30002  # port on minikube host
